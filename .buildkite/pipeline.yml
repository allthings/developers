steps:
  - trigger: documentation
    label: ":rocket: building + deploying documentation"
    branches: "master"
    async: true
    build:
      commit: "$BUILDKITE_PIPELINE_SLUG / $BUILDKITE_COMMIT"
      branch: "$BUILDKITE_BRANCH"
      message: "$BUILDKITE_MESSAGE"
